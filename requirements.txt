# --- Core scientific stack ---
numpy>=1.24,<2.2
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=12.0            # parquet caching

# --- Data acquisition ---
yfinance>=0.2.40

# --- Deep learning (CPU wheels resolve automatically; install the CUDA
#     build from pytorch.org if you have a GPU) ---
torch>=2.1

# --- Hyper-parameter optimisation (optional but recommended) ---
optuna>=3.4

# --- Classical baseline (optional) ---
statsmodels>=0.14

# --- Optional heavy extras -------------------------------------------------
# Temporal Fusion Transformer: uncomment to enable src/models/tft.py
# pytorch-forecasting>=1.0
# lightning>=2.1
#
# News sentiment (FinBERT) for the optional sentiment feature:
# transformers>=4.40
# torch already covers the backend
